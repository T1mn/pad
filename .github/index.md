# .github

- `workflows/`：仓库级 GitHub Actions 配置。
- `release.yml`：GitHub 自动生成 release notes 的分组配置。
- `dependabot.yml`：cargo 与 github-actions 的每周依赖更新。
- `ISSUE_TEMPLATE/`：issue forms，并把安全问题导向 private advisory。
- `PULL_REQUEST_TEMPLATE.md`：PR 勾选项，对应 CI 强制的结构约束。
