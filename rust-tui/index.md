# rust-tui

- `Cargo.toml`：Rust crate 配置与依赖入口。
- `src/`：主程序源码。
- `assets/`：内置 prompt 与静态资源。
- `scripts/`：crate 内部构建 / CI 辅助脚本。
- `.github/workflows/`：crate 级 workflow 配置。
- `rust-toolchain.toml`：工具链约束。
