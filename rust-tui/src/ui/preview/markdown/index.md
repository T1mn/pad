# markdown

- `style.rs`：Markdown stylesheet、inline code 颜色与 options。
- `normalize.rs`：session detail Markdown 段落补空行与结构行识别。
- `render.rs`：detail surface、separator、padding/content line 渲染。
- `wrap.rs`：styled line 换行、surface 补色与平滑滚动扁平化。
- `inline.rs` / `inline_tests.rs`：inline code token 化与普通预览行着色。
