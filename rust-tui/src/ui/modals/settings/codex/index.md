# settings/codex

- `mod.rs`：Codex settings 详情入口，按当前二级目录渲染列表。
- `items.rs`：Codex settings 列表聚合与通用列表项辅助。
- `categories.rs`：二级目录标题与摘要。
- `options.rs`：runtime、status、prompt、preview、CLI 子项列表。
- `version.rs`：Codex CLI 版本状态摘要。
