# ui/selection

- `mod.rs`: shared selection facade.
- `model.rs` / `model_tests.rs`: shared selection item data and query matching.
- `state.rs`: selection cursor, scroll and query state.
- `render.rs` / `render/`: selection surface sizing, headers and list row rendering.
- `row.rs` / `row/`: row title/value rendering, styles and width truncation helpers.
