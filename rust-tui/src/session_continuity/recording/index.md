# session_continuity/recording

- `../recording.rs` 内联 `hook`：记录 agent hook 事件、bootstrap 状态和 hook 诊断。
- `../recording.rs` 内联 `cache`：记录 hook/resolver 写入缓存后的 continuity 状态。
- `../recording.rs` 内联 `preview`：记录预览 fallback assessment 与相关诊断。
