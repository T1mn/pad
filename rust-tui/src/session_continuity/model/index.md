# session_continuity/model

- `enums.rs`：continuity health、attempt classification 与写入来源枚举。
- `ledger.rs`：落盘 ledger 与 session record 结构。
- `snapshot.rs`：对外 snapshot 结构与 record 转换。
- `fallback.rs`：预览 fallback 输入与决策结果。
- `diagnostic.rs`：诊断事件序列化结构。
