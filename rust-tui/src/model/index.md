# model

- `../model.rs`：模型模块入口，保留 `crate::model::*` 对外路径。
- `../model.rs` 内联 `agent`：agent 类型与运行状态模型。
- `../model.rs` 内联 `preview`：preview 来源、视图、turn 与共享 turn 列表。
- `../model.rs`：native agent live pane、hook metadata 与 preview cache 数据模型。
