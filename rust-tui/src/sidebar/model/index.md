# sidebar/model

- `thread.rs`：`SidebarThread` 与 thread 级 helper。
- `folder.rs`：`SidebarFolder`、folder summary 与 primary thread 选择。
- `item.rs`：sidebar 可见项 enum 与访问 helper。
- `activity.rs`：activity override 与排序 activity key 生成。
