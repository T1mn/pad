# sidebar/model

- `../model.rs` 内联 `thread`：`SidebarThread` 与 thread 级 helper。
- `../model.rs` 内联 `folder`：`SidebarFolder`、folder summary 与 primary thread 选择。
- `../model.rs` 内联 `item`：sidebar 可见项 enum 与访问 helper。
- `../model.rs` 内联 `activity`：activity override 与排序 activity key 生成。
