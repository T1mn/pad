# trash/thread

- `codex.rs`：从 Codex history thread ref 构建 deleted `SidebarThread`，过滤 subagent。
- `claude.rs`：从 Claude history thread ref 构建 deleted `SidebarThread`。
- `gemini.rs`：从 Gemini history thread ref 构建 deleted `SidebarThread`，过滤 subagent。
- `opencode.rs`：从 OpenCode history thread ref 构建 deleted `SidebarThread`。
