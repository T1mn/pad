# trash/thread

- `../thread.rs` 内联 `codex`：从 Codex history thread ref 构建 deleted `SidebarThread`，过滤 subagent。
- `../thread.rs` 内联 `claude`：从 Claude history thread ref 构建 deleted `SidebarThread`。
- `../thread.rs` 内联 `gemini`：从 Gemini history thread ref 构建 deleted `SidebarThread`，过滤 subagent。
- `../thread.rs` 内联 `opencode`：从 OpenCode history thread ref 构建 deleted `SidebarThread`。
