# session_target

- `resolve.rs`：会话目标解析、transcript 路径定位与 persistence panel 组装。
- `sources.rs`：Codex / Claude / Gemini / OpenCode 的线程与路径辅助查询。
- `target.rs`：`SessionTarget` 结构体。
- `tests.rs`：Gemini 会话 ID、resolved session、persistence panel 回归测试。
