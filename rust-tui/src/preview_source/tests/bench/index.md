# preview_source/tests/bench

- `env.rs`：读取 `PAD_PREVIEW_BENCH_*` 环境变量。
- `runner.rs`：单个 transcript 的 preview load 分段 benchmark。
- `report.rs`：benchmark 摘要输出。
- `rollout.rs`：Codex rollout transcript 文件名里的 session id 解析与单测。
