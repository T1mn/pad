# relay/tests

- `provider_configs.rs` / `provider_configs/`：按 Codex、Claude、DeepSeek、Gemini、OpenCode 分组的 provider relay 配置测试。
- `runtime_overlays.rs`：runtime overlay 测试入口。
- `runtime_overlays/`：Codex/Claude 权限、状态栏、prompt、组合覆盖测试。
