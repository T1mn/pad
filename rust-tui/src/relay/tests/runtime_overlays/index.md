# relay/tests/runtime_overlays

- `codex_permissions.rs`：Codex full access 覆盖与恢复测试。
- `codex_features.rs` / `codex_features/`：Codex fast/goals/multi-agent/web-search 覆盖测试。
- `codex_status_line.rs`：Codex status_line 覆盖测试。
- `codex_prompts.rs`：Codex prompt candidate 覆盖测试。
- `combined_overlays.rs`：Codex 多覆盖组合测试。
- `claude_permissions.rs`：Claude full access 覆盖与恢复测试。
