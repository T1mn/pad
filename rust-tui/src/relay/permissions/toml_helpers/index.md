# permissions/toml_helpers

- `../toml_helpers.rs` 内联 `set`：创建嵌套 TOML table，并写入 bool / string array。
- `../toml_helpers.rs` 内联 `get`：按路径读取 bool / string array。
- `../toml_helpers.rs` 内联 `restore`：根据保存的 JSON 状态恢复 TOML 字段或路径。
- `../toml_helpers.rs` 内联 `remove`：删除路径并清理空 table。
