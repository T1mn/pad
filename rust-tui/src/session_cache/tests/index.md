# session_cache/tests

- `support.rs`：共享测试 panel 构造器。
- `turns.rs`：最近对话 turn 合并规则测试。
- `bindings.rs`：pane/session binding 查找与 fallback 匹配测试。
- `persist.rs`：hook 持久化的 session 隔离与同 session fallback 测试。
- `preload.rs`：scan preload、snapshot 应用和 busy 状态恢复测试。
