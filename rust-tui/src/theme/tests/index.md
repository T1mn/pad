# theme/tests

- `../tests.rs` 内联 `support`：共享临时 HOME helper。
- `../tests.rs` 内联 `config`：配置保存/读取、legacy 字段、默认权限和路径解析测试。
- `../tests.rs` 内联 `sound`：sound section roundtrip 与非法 preset 归一化测试。
- `../tests.rs` 内联 `provider`：Codex provider base URL 候选和首选 URL 测试。
- `../tests.rs` 内联 `palette`：主题可读性增强颜色回归测试。
