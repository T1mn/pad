# theme/tests

- `support.rs`：共享临时 HOME helper。
- `config.rs`：配置保存/读取、legacy 字段、默认权限和路径解析测试。
- `sound.rs`：sound section roundtrip 与非法 preset 归一化测试。
- `provider.rs`：Codex provider base URL 候选和首选 URL 测试。
- `palette.rs`：主题可读性增强颜色回归测试。
