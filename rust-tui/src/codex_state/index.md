# codex_state

- `query.rs`：状态查询入口。
- `cache.rs` / `archive.rs`：缓存与归档处理。
- `migration.rs`：把 PAD 专用 Codex Home 写入的 rollout 路径规范回官方 `~/.codex` 路径。
- `pathing.rs`：路径定位。
- `model.rs`：状态模型。
- `util.rs`：辅助函数。
- `tests.rs`：测试。
