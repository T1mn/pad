# codex_turn_diff/storage

- `../storage.rs` 内联 `pending`：pending baseline 的保存、匹配、删除与列表读取。
- `../storage.rs` 内联 `completed`：completed record、patch 文件与 JSONL index 持久化。
- `../storage.rs` 内联 `json`：通用 JSON 文件读写与目录扫描。
