# ui

- `mod.rs`：pad sider TUI 主循环与按键分发。
- `render.rs`：主布局渲染，包含 tree、index map 与 changes。
- `overlay.rs`：全屏 Markdown 预览、搜索弹层与快捷键帮助。
- `markdown.rs` / `markdown/`：基于 `pulldown-cmark` 的自定义 Markdown 渲染。
