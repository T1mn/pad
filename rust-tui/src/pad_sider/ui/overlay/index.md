# overlay

- `../overlay.rs` 内联 `preview`：全屏文件预览弹层，复用右侧预览渲染与行号/缩放选项。
- `../overlay.rs` 内联 `search`：`/` 文件搜索弹层、输入框、匹配列表和选中项样式。
- `../overlay.rs` 内联 `help`：`?` 快捷键帮助弹层内容与边框。
- `../overlay.rs` 内联 `layout`：弹层居中区域和聚焦边框的小工具。
