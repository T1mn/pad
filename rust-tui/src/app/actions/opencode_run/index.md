# app/actions/opencode_run

- `../opencode_run.rs` 内联 `command`：组装并启动 `tmux new-window` 中的 `opencode run` 命令。
- `../opencode_run.rs` 内联 `prompt`：剪贴板 prompt 规范化与 toast 预览文本。
- `../opencode_run.rs` 内联 `text`：OpenCode run 动作 toast 文案。
