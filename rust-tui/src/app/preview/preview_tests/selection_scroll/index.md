# app/preview/preview_tests/selection_scroll

- `detail.rs`：同一 session detail selection/scroll 保留与 turn 缩短时收敛测试。
- `plain.rs`：plain preview follow-bottom/follow-selection 目标切换测试。
- `panel_cache.rs`：panel session cache state 变化触发 dirty 测试。
- `context.rs`：preview context 切换时选择和滚动重置测试。
