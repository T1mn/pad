# relay_settings/actions

- `../actions.rs` 内联 `agent_list`：agent 列表导航与进入 provider 列表。
- `provider_list.rs`：provider 列表导航、启用切换、增删与 OpenCode 模型弹层入口。
- `../actions.rs` 内联 `detail_pane`：详情字段导航、编辑入口、provider 测试与 Codex provider 导入导出。
