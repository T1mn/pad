# event/mouse

- `../mouse.rs` 内联 `regions`：normal 模式鼠标区域计算。
- `../mouse.rs` 内联 `hit_test`：矩形、sidebar 行与 preview turn 命中测试。
- `click.rs`：左键点击分发，包含 sidebar、preview 信息卡与 turn 选择。
- `../mouse.rs` 内联 `selection`：preview 鼠标拖选与复制。
- `../mouse.rs` 内联 `hover`：folder hover 状态更新。
- `scroll.rs`：滚轮合并、方向换算与普通模式滚动处理。
