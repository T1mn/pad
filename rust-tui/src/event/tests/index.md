# event/tests

- `mod.rs`：测试公共样例、按键和鼠标事件构造。
- `mod.rs` 内联 `mouse_tests`：普通模式鼠标选择、预览点击和滚轮行为。
- `mod.rs` 内联 `sidebar_keys`：空格、双空格、j/k、数字跳转与 native live entry 跨 tab 聚焦行为。
- `mod.rs` 内联 `preview_tab`：预览详情页 Tab / 双 Tab 行为。
